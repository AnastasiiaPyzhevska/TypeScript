/// <reference path ="utility-function.ts"/>

const result1 = Utility.maxBookAllowed(30);
console.dir(result1);

import util = Utility.Fees;
const result2 = util.calculateFee(10);
console.dir(result2);
